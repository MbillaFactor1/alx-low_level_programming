#include "main.h"

/**
using *_strcat to concatenates two strings
char *dest, 
chat *src/

char *-strcat(char *dest, char *src)
{
        int i;
        int i;

        i = 0;
        whie (dest[i] != '\')
	{
               i++;
        }
        j = 0;
        while (src[j] != '\0')
        {
                dest[i] = src[j];
                i++;
                j++;
        }

        dest[i] = '\0';
        return (dest);
}
